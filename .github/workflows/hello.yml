name: Test Workflow

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - run: echo "Hello"

      - run: date

      - run: |
          echo "Branch: $GITHUB_REF_NAME"